#!/bin/bash
set -e

# Update icon and desktop caches (non-blocking, ignore errors)
(gtk-update-icon-cache /usr/share/icons/hicolor || true) &
(update-desktop-database /usr/share/applications || true) &

exit 0
